#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include <math.h>
int main()
{
	
	srand(time(NULL));
	
	double n=7;
	double dist=0,a,b;
	double alpha,prob=0;
	double e,f1,f2,T;
	
	(rand()%7)+1;
	(rand()%7)+1;
	(rand()%7)+1;
	
	dist=sqrt(pow(a-b,2));
	
	alpha=rand()%1;
	
	prob=pow(e,(f1-f2)/T);
	
	T=alpha*T;
	
}
